<Window x:Class="WPFChatClient.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="WPF Chat Client" Height="450" Width="600">
    <Window.Resources>
        <!-- Style cho các button emoji -->
        <Style x:Key="EmojiButtonStyle" TargetType="Button">
            <Setter Property="Width" Value="40"/>
            <Setter Property="Height" Value="40"/>
            <Setter Property="FontSize" Value="20"/>
            <Setter Property="Margin" Value="0"/>
            <Setter Property="Padding" Value="0"/>
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderBrush" Value="LightGray"/>
            <Setter Property="BorderThickness" Value="0.5"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="LightBlue"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal" Grid.Row="0" Margin="0,0,0,10">
            <TextBlock Text="Tên bạn:" VerticalAlignment="Center" Margin="0,0,10,0" FontWeight="Bold"/>
            <TextBox x:Name="txtUser" Width="150" Padding="5"/>
            <Button x:Name="btnConnect" Content="Kết nối Server" Click="btnConnect_Click" Margin="10,0,0,0" Padding="10,5" Background="#DDDDDD"/>
        </StackPanel>

        <ListBox x:Name="lstChat" Grid.Row="1" Margin="0,0,0,10" Background="#F0F0F0" BorderBrush="#CCCCCC"/>

        <Grid Grid.Row="2" Margin="0,5,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <Button x:Name="btnEmoji" Content="☺" Grid.Column="0" Width="30" Margin="0,0,5,0" Click="btnEmoji_Click"/>
            
            <Popup x:Name="popEmoji" PlacementTarget="{Binding ElementName=btnEmoji}" Placement="Top" StaysOpen="False">
                <Border Background="White" BorderBrush="Gray" BorderThickness="1" CornerRadius="5" Padding="5">
                    <UniformGrid x:Name="emojiGrid" Columns="10"/>
                </Border>
            </Popup>

            <TextBox x:Name="txtMessage" Grid.Column="1" Padding="5"/>

            <Button x:Name="btnSend" Content="Gửi" Grid.Column="2" Width="80" Click="btnSend_Click" IsEnabled="False" Margin="5,0,0,0" Background="#007ACC" Foreground="White" FontWeight="Bold"/>
        </Grid>


    </Grid>
</Window>